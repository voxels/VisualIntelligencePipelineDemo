name: VisualIntelligenceMac
options:
  bundleIdPrefix: com.secretatomics.visualintelligence
  deploymentTarget:
    macOS: "26.0"
  xcodeVersion: "26.0"
  createIntermediateGroups: true
  defaultConfig: Debug

packages:
  DiverKit:
    path: ../DiverKit
  DiverShared:
    path: ../DiverShared
  knowmaps:
    url: https://github.com/voxels/Know-Maps.git
    branch: main

targets:

  # ── Main macOS companion app ──────────────────────────────────────────────
  VisualIntelligenceMac:
    type: application
    platform: macOS
    sources:
      - path: Sources
        excludes: []
      - path: Sources/App/Assets.xcassets
        type: file
    dependencies:
      - package: DiverKit
      - package: DiverShared
      - sdk: ServiceManagement.framework
      # Embed the EdgeHelper as a Login Item helper
      - target: VisualIntelligenceEdgeHelper
        embed: true
        codeSign: true
        subpath: Contents/Library/LoginItems
    info:
      path: Info.plist
      properties:
        CFBundleDisplayName: Visual Intelligence
        CFBundleShortVersionString: "1.2"
        CFBundleVersion: "1"
        LSApplicationCategoryType: public.app-category.productivity
        NSLocalNetworkUsageDescription: >
          Visual Intelligence communicates with the Edge Node helper on your
          local network to offload ML inference to your Mac.
        NSBonjourServices:
          - _visualintel._tcp
        SMPrivilegedExecutables: {}   # not needed — loginItem doesn't require this
    settings:
      base:
        PRODUCT_NAME: "Visual Intelligence"
        PRODUCT_BUNDLE_IDENTIFIER: com.secretatomics.visualintelligence.mac
        SWIFT_VERSION: "6.0"
        MACOSX_DEPLOYMENT_TARGET: "26.0"
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_ENTITLEMENTS: VisualIntelligenceMac.entitlements
        DEVELOPMENT_TEAM: "SDZ7V7QF9F"
        ENABLE_HARDENED_RUNTIME: YES
        SWIFT_STRICT_CONCURRENCY: complete
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  # ── EdgeHelper Login Item helper — embedded inside the main app ─────────────
  VisualIntelligenceEdgeHelper:
    type: application
    platform: macOS
    sources:
      - path: ../EdgeDaemon
        excludes:
          - project.yml
          - "*.xcodeproj"
          - "*.xcworkspace"
          - "*.entitlements"
          - Info.plist
          - build.log
          - export
          - run_daemon.sh
          - README.md
    dependencies:
      - package: DiverKit
      - package: DiverShared
    info:
      path: ../EdgeDaemon/Info.plist
      properties:
        CFBundleDisplayName: "Visual Intelligence Edge Helper"
        CFBundleIdentifier: com.secretatomics.visualintelligence.mac.edgenode.helper
        LSUIElement: true
        LSApplicationCategoryType: public.app-category.utilities
        NSLocalNetworkUsageDescription: >
          The Edge Node advertises via Bonjour to serve ML inference
          to Visual Intelligence on your iPhone.
        NSBonjourServices:
          - _visualintel._tcp
    settings:
      base:
        PRODUCT_NAME: "VisualIntelligenceEdgeHelper"
        # Must be child of the parent app for SMAppService.loginItem to work
        PRODUCT_BUNDLE_IDENTIFIER: com.secretatomics.visualintelligence.mac.edgenode.helper
        SWIFT_VERSION: "6.0"
        MACOSX_DEPLOYMENT_TARGET: "26.0"
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_ENTITLEMENTS: ../EdgeDaemon/EdgeDaemon.entitlements
        SKIP_INSTALL: YES
        DEVELOPMENT_TEAM: "SDZ7V7QF9F"
        ENABLE_HARDENED_RUNTIME: YES
        SWIFT_STRICT_CONCURRENCY: complete

schemes:
  VisualIntelligenceMac:
    build:
      targets:
        VisualIntelligenceMac: all
        VisualIntelligenceEdgeHelper: all
    run:
      config: Debug
    profile:
      config: Release
